{% extends 'base.html.twig' %}


{% block content %}
	{% include 'frag/adminSite.frag.html.twig' %}
	{#template de la navbar #}

	<div class="container">


		<h1>Creation d'une categorie</h1>


		{# aller sur cette page :https://symfony.com/doc/current/form/form_customization.html #}
		{# {{form(form)}} #}

		{{ form_start(form) }}

		
		<div class="form-group">
			{{ form_label(form.CategoryImages) }}
			{{ form_widget(form.CategoryImages) }}
			<div class="form-error">
				{{ form_errors(form.CategoryImages) }}
			</div>
		</div>
		<div class="form-group">
			{{ form_label(form.NameCategory) }}
			{{ form_widget(form.NameCategory) }}
			<div class="form-error">
				{{ form_errors(form.NameCategory) }}
			</div>
		</div>
		<div class="form-group">
			{{form_row(form.submit)}}
		</div>


		{{ form_end(form) }}
	</div>
{% endblock %}